import { _decorator, Component } from 'cc';

const { ccclass } = _decorator;

/**
 * CameraAdapter - 相机适配器（已废弃，通过编辑器配置控制屏幕适配）
 */
@ccclass('CameraAdapter')
export class CameraAdapter extends Component {
  // 屏幕适配逻辑已移至编辑器配置（Canvas 组件）
  // 此脚本仅保留空壳，可安全删除
}
